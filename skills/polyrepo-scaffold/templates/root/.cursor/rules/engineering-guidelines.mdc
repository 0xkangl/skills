---
description: Engineering behavior guidelines to reduce common LLM coding mistakes. Biases toward caution over speed. For trivial tasks, use judgment.
globs: ["**/*"]
alwaysApply: true
---

Follow the project rules and conventions in `{{PROJECT}}-spec-center/AGENTS.md`.

Convention documents live under `{{PROJECT}}-spec-center/conventions/` (starts empty — populate as the project adopts them, then `@import` them here).
