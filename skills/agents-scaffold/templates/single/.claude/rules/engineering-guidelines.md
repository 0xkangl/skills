# Engineering Guidelines

Follow the project rules and conventions in the repo root `AGENTS.md`.

Universal conventions (HTTP/API, observability, testing, error codes) are **not** copied into `docs/` — reference the `code-conventions` skill at runtime. `docs/` holds project-private convention documents only; add an index entry in `AGENTS.md` when one is added.
